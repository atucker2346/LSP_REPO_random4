# Question 2 — AI Usage

**AI Tools Used:**  
Cursor 

**Prompts Used (2–5 max):**

1. Implement final exam Question 2 Template Method: Report, StudentReport, CourseReport, Driver, expected output.

**How AI Helped (2–3 sentences):**  
AI scaffolded the class hierarchy, `generateReport()` structure, and `Driver` loop. I verified the output format and naming against the exam rubric.

**Reflection (1–2 sentences):**  
I confirmed that `final` on the template method prevents subclasses from breaking the workflow.
